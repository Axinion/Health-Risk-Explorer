services:
  - type: web
    name: health-risk-explorer
    runtime: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
    healthCheckPath: /_stcore/health
    autoDeploy: true
